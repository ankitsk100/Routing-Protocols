# Router Configs

Each file contains the configuration for one router.

## Files
- `R1.txt`
- `R2.txt`
- `R3.txt`
- `R4.txt`

## Usage
Open Packet Tracer → Router CLI → paste config (or compare with running-config).
